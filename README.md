# vscode-google-nepali-input

`"ctrl+space"` to enable/disable input

`"1"-"8"` to choose th n-th

`"space"` to choose the first

`"enter"` to input the english

`"-"`, `"+"` to turn to the prev/next page
